#include <stdio.h>

int main() {
    int a = 5, b = 2, c = 4, d = 6, e;
    e = (a * c) + (d * b);
    printf("A multiplicação entre %d e %d mais o resultado da outra multiplicação entre %d e %d, o total é igual a %d\n", a, c, d, b,e);

    return 0;
}