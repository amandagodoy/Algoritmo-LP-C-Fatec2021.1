#include <stdio.h>

int main() {
    float a = 12.2, b = 3.1, c = 4.5, e;
    e = (12.2 - 3.1) / 4.5;
    printf("A subtração entre %.2f e %.2f, vai ser divido por %.2f assim o resultado é igual a %.2f\n", a, b, c, e);

    return 0;
}