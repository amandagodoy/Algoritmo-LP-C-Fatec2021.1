#include <stdio.h>

int main() {
    int a = 6, b = 18, c = 2, e;
    e = (6 + 18) / 2;
    printf("A soma entre %d e %d, vai ser divido por %d assim o resultado é igual a %d\n", a, b, c, e);

    return 0;
}