#include <stdio.h>

int main() {
    int a=5, b=2, c;
    c = a + b;

    float x=2.5, y=0.3, Z;
    printf("soma = %d\n", c);

    printf("%d - %d = %d\n", a, b, a-b);

    printf("mutiplicação: %.2f * %.2f = %.2f\n", x, y, x*y);

    //printf("o valor e %d\n", c*a);
    return 0;
}